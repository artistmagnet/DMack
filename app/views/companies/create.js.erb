// window.location.reload();
$("#myModal").modal("hide");
$("#add-new-company").hide();
$(".company_name").val("<%=@company.name%>")
$('.flash-message').html("<%= escape_javascript render('layouts/includes/notice') %>");
