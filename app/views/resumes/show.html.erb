
<div style="background-color: #CCCCCC; border-radius: 4px; padding: 10px 20px">
  <h3>Theater</h3>

  <p>
    <strong>Production</strong>
    <%= link_to @resume.roles.first.production_name, @resume.roles.first.production %>
  </p>

  <p>
    <strong>Role</strong>
    <%= @resume.roles.first.name %>
  </p>

  <p>
    <strong>Venue</strong>
    <%= link_to @resume.roles.first.production.shows.first.venue_name, @resume.roles.first.production.shows.first.venue  %>
  </p>

  <p>
    <strong>Company</strong>
    <%= link_to @resume.roles.first.production.company_name, @resume.roles.first.production.company %>
  </p>

  <p>
    <strong>Dates</strong>
    <%= @resume.roles.first.production.shows.first.date %>
  </p>
</div>

<%= link_to 'Edit', edit_resume_path(@resume) %> |
<%= link_to 'Resume list', resumes_path %>
