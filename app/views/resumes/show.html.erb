<div class="container-fluid m-top-20">
  <div class="row">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-body" id="show-resume">         
          <section class="list-resumes">            
            <div class="row">
                
              <div class="col-md-3 resume_attribute">
               <%= show_attribute(@resume)%>
               
               </div>

               
              <div class="col-md-4 contact_block">
                 <h2><%=@resume.contact_info.nick_name.upcase%></h2>
                 <% if @resume.try(:performer_type).present?%>
                  <h4><%=@resume.try(:performer_type).upcase%></h4>
                 <%end%>
                 <% if @resume.try(:union_guild).present?%>
                  <h5><%=@resume.try(:union_guild).upcase%></h5>
                <%end%>

                <br/>
                 <% if (@resume.image.file? && @resume.resume_type == 'Performing Artist') || ( !@resume.image.file? && (@resume.resume_type == 'Production Member' || @resume.resume_type == 'Director'))%>
 
                  
                
                  
                   <%= render 'resumes/show_contact_info',:@resume=>@resume%>
                 <%end%>
                
               </div>

                <div class="col-md-4">
                 <%=show_image(@resume)%>
               </div>
              
                
             </div>
                   
            </section>
          
            <!-- <hr/> -->
             <section class="list-resumes">
              <div class="row">
                <strong><div class="col-md-2"><%=@resume.resume_sections.where(:section_id=>1).first.try(:section_name)%>:</div></strong>
              
                     <% @resume.representations.each do |representation|%>
                     
                       <% representation.rcolumns.each do |col|%>
                      <div class="col-md-2"><%= col.capitalize %></div>
                      <%end%>
              </div>
                      <%end%>                 
              </section><br />



            <section class="list-resumes">
              <strong>THEATRE</strong>
              <%=render 'resumes/nested/theatre_fields'%>
                <% @resume.roles.each do |theatre|%>
                <div class="row">
                  <div class="col-md-3"><%= link_to theatre.production_name.upcase,theatre.production ,:target=>"_blank" if !theatre.production.blank?%></div>
                  <div class="col-md-2"><%= theatre.name.capitalize%></div>
                  <div class="col-md-2"><%= theatre.director_name.capitalize%></div>
                  <div class="col-md-4"><%= get_venue_and_company(theatre) %></div>
                </div>
                <%end%>
              
            </section ><br/>
            
            <section class="list-resumes">
              <strong><%=@resume.resume_sections.where(:section_id=>2).first.try(:section_name)%></strong>
              
                   <% @resume.educations.each do |education|%>
                    <div class="row">
                      <% education.ecolumns.each do |col|%>
                      <div class="col-md-2" id="<%=education.is_bold.present? ? "bold_col" : ""%>"><%= col.capitalize %></div>
                      <%end%>
                    </div> 
                    <%end%>                 
            </section><br/>
     
            <!-- <%#if @resume.resume_type== "Performing Artist"%> -->
             <!--  <section class="list-resumes">
                <h3>Attributes:</h3>
                  <div data-example-id="striped-table" class="bs-example">
                    <table class="table table-striped">
                      <thead>
                        <tr>
                          <th>Gender</th>
                          <th>Age</th>
                          <th>Height</th>
                          <th>Weight</th>
                          <th>Hair color</th>
                          <th>Hair Lenght</th>
                          <th>Eye color</th>
                          <th>Vocal Range</th>
                          <th>Ethnicity</th>

                        </tr>
                      </thead>
                      <tbody>
                        <tr>
                          <td><%#= @resume.try(:resume_attribute).try(:gender) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:age) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:height) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:weight) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:hair_color) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:hair_lenght) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:eye_color) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:vocal_range) %></td>
                          <td><%#= @resume.try(:resume_attribute).try(:ethnicity) %></td>
                        </tr>                  
                      </tbody>
                    </table>
                  </div> 
                </section> -->
              <!-- <%#end%><br/> -->
            
           <!--  <h3>Contact Info</h3>
            <div data-example-id="striped-table" class="bs-example">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>Nick Name</th>
                    <th>First Name</th>
                    <th>Middle Name</th>
                    <th>Last Name</th>
                    <th>Suffix</th>
                    <th>Street Address 2</th>
                    <th>street Address 2</th>
                    <th>city</th>
                    <th>State</th>
                    <th>Zip Code</th>
                    <th>Country</th>
                    <th>Email</th>
                    <th>Website</th>
                    <th>Facebook</th>
                    <th>Twitter</th>
                    <th>Linkedin</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td><%= @resume.contact_info.nick_name %></td>
                    <td><%= @resume.contact_info.first_name %></td>
                    <td><%= @resume.contact_info.middle_name %></td>
                    <td><%= @resume.contact_info.last_name %></td>
                    <td><%= @resume.contact_info.suffix %></td>
                    <td><%= @resume.contact_info.street_address1 %></td>
                    <td><%= @resume.contact_info.street_address2 %></td>
                    <td><%= @resume.contact_info.city %></td>
                    <td><%= @resume.contact_info.state %></td>
                    <td><%= @resume.contact_info.zip_code %></td>
                    <td><%= @resume.contact_info.country %></td>
                    <td><%= @resume.contact_info.email %></td>
                    <td><%= @resume.contact_info.website %></td>
                    <td><%= @resume.contact_info.facebook %></td>
                    <td><%= @resume.contact_info.twitter %></td>
                    <td><%= @resume.contact_info.linkedin %></td>
                  </tr>              
                </tbody>
              </table>
            </div> -->     
                
                
             

              <section class="list-resumes">
                <strong><%=@resume.resume_sections.where(:section_id=>3).first.try(:section_name)%></strong>
                <div data-example-id="striped-table" class="bs-example">
                <p>
                    
                       <%#= skill.category.try(:name) %>
                        <li style="list-style-type: none;"><%= @resume.skills.map { |tag| tag.skills.capitalize }.join(', ')  if !@resume.skills.blank?%></li>
                     
                </p>
                </div>
              </section><br />

              <section class="list-resumes">
              <strong><%=@resume.resume_sections.where(:section_id=>4).first.try(:section_name)%></strong>
             
                  <%#=render 'resumes/nested/final_education_fields',:e=>@resume,:cust=>"custom"%>
                   <% @resume.customs.each do |custom|%>
                    <div class="row">
                     <% custom.ccolumns.each do |col|%>
                      <div class="col-md-2" id="<%=custom.is_bold.present? ? "bold_col" : ""%>"><%= col.capitalize %></div>
                      <%end%>
                    </div> 
                    <%end%>
            </section><br/>

            <section class="list-resumes">
              <strong><%=@resume.resume_sections.where(:section_id=>5).first.try(:section_name)%></strong>
           
            <li style="list-style-type: none;"><%= @resume.others.map { |tag| tag.content.capitalize }.join(', ')  if !@resume.others.blank?%></li>
            </section><br />

            </div>
            
          </div>

        </div>
      </div>
    </div>
  </div>
</div>

<div class="container-fluid" id="section_slot" data-update-url="<%= sort_section_slots_url %>">
  <%# @resume.section_slots.each do |section_slot| %>
    <%#= render partial: 'section_slot', locals: {section_slot: section_slot} %>
  <%# end %>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <!--<button type="button" class="btn btn-primary m-top-m6">Add a skills section</button>-->
      <%#= link_to add_resume_table_path(@resume), method: :post do %>
        <!-- <button type="button" class="btn btn-primary m-top-m6">Add a custom section</button> -->
      <%# end %>
    </div>
  </div>
</div>
<style type="text/css">
  .table-striped > tbody > tr:nth-child(2n+1) > td, .table-striped > tbody > tr:nth-child(2n+1) > th{
     background-color: white ;
  }
</style>